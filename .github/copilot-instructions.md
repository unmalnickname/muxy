

## Auto-generated signatures
<!-- Updated by gen-context.js -->
<!-- Generated by SigMap gen-context.js v6.10.1 -->
<!-- Updated: 2026-05-11T01:00:10.782Z -->
<!-- Do not edit below — regenerate with: node gen-context.js -->

## SigMap commands

| When | Command |
|------|---------|
| Before answering a question about code | `sigmap ask "<your question>"` |
| After changing config or source dirs | `sigmap validate` |
| To verify an AI answer is grounded | `sigmap judge --response <file>` |

# Code signatures

## changes (last 5 commits — 2 days ago)
```
Muxy/Models/PipelineState.swift               +PipelineState  +assignSeverity  +checkStepOrdering  +checkDependencyViolations
Muxy/Models/ProjectHealthState.swift          +ProjectHealthState  +installWatcher  +observeRemoteChanges  ~ProjectHealthState
Muxy/Services/ProjectOpenService.swift        +isGitURL  +cloneAndAdd  +createNamedProject  +browseAndAdd
Muxy/Views/Health/PipelinePanel.swift         +dependencyViolationsSection  +severityBadge  +stepIcon  ~stepIcon
```

## .agents

### .agents/skills/agent-workflow/SKILL.md
```
h1 Agent Workflow
h2 Invocation
h2 Template
h2 Branch Strategy
h2 Setup (agents do this automatically)
h2 Working on Projects WITHOUT the Workflow Setup
h2 UI Development Protocol
h2 Full Pipeline
h2 Who Uses What
code-fence plain
code-fence bash
```

## Muxy

### Muxy/Services/ApprovedDevicesStore.swift
```
struct ApprovedDevice
class ApprovedDevicesStore
  func approve(deviceID, name, token)
  func validate(deviceID, token) → ApprovedDevice?
  func touch(deviceID)
  func rename(deviceID, to newName)
  func revoke(deviceID)
  func hash(_ token) → String
```

### Muxy/Services/CLIAccessor.swift
```
enum CLIAccessor
  func openProjectFromPath(_ path, appState, projectStore, worktreeStore)
  func installCLI()
```

### Muxy/Services/ClaudeUsageParser.swift
```
enum ClaudeUsageParserError
enum ClaudeUsageParser
  func parseMetricRows(from data) → [AIUsageMetricRow]
```

### Muxy/Services/CodableFileStore.swift
```
struct CodableFileStoreOptions
struct CodableFileStore
  func load() → Value?
  func save(_ value)
  func remove()
```

### Muxy/Services/CodexUsageParser.swift
```
enum CodexUsageParser
  func parseMetricRows(from data) → [AIUsageMetricRow]
```

### Muxy/Services/CommandShortcutStore.swift
```
protocol CommandShortcutPersisting
  func loadConfiguration() → CommandShortcutConfigu...
  func saveConfiguration(_ configuration)
class FileCommandShortcutPersistence
  func loadConfiguration() → CommandShortcutConfigu...
  func saveConfiguration(_ configuration)
struct CommandShortcutConfiguration
class CommandShortcutStore
  func addShortcut() → CommandShortcut
  func updateShortcut(_ shortcut)
  func deleteShortcut(id)
  func deleteAllShortcuts()
  func updatePrefixCombo(_ combo)
  func resetPrefixCombo()
  func activateLayer()
  func deactivateLayer()
```

### Muxy/Services/CopilotUsageParser.swift
```
enum CopilotUsageParser
  func parseMetricRows(from data) → [AIUsageMetricRow]
```

### Muxy/Services/DroppedPathsParser.swift
```
enum DroppedPathsParser
  func parse(fileURLs, plainString, fileExists) → Bool =
```

### Muxy/Services/FactoryUsageParser.swift
```
enum FactoryUsageParserError
enum FactoryUsageParser
  func parse(from data) → ParsedUsage
```

### Muxy/Services/FileClipboard.swift
```
enum FileClipboard
  func write(paths, isCut)
  func read() → Contents?
  func clearCutMarker()
```

### Muxy/Services/FilePermissions.swift
```
enum FilePermissions
```

### Muxy/Services/FileSearchService.swift
```
struct FileSearchResult
enum FileSearchService
  async func search(query, in projectPath) → [FileSearchResult]
```

### Muxy/Services/FileSystemOperations.swift
```
enum FileSystemOperationError
enum FileSystemOperations
  async func createFile(named rawName, in directory) → String
  async func createFolder(named rawName, in directory) → String
  async func rename(at absolutePath, to rawName) → String
  async func moveToTrash(_ absolutePaths)
  async func move(_ sources, into destinationDirectory) → [String]
  async func copy(_ sources, into destinationDirectory) → [String]
  func isInside(path, ancestor) → Bool
```

### Muxy/Services/FileTreeService.swift
```
struct FileTreeEntry
enum FileTreeService
  async func loadChildren(of directoryAbsolutePath, repoRoot) → [FileTreeEntry]
```

### Muxy/Services/Git/DiffLoader.swift
```
enum DiffLoader
  func load(_ request, cache, git)
```

### Muxy/Services/Git/GitCommitLogParser.swift
```
enum GitCommitLogParser
  func parseCommitLog(_ raw) → [GitCommit]
  func parseRefs(_ raw) → [GitRef]
```

### Muxy/Services/Git/GitDiffParser.swift
```
struct ParsedDiffRows
enum GitDiffParser
  func parseRows(_ patch) → ParsedDiffRows
  func collapseContextRows(_ rows) → [DiffDisplayRow]
```

### Muxy/Services/Git/GitModels.swift
```
struct NumstatEntry
struct GitStatusFile
struct GitCommit
struct GitRef
struct DiffDisplayRow
```

### Muxy/Services/Git/GitPRParser.swift
```
enum GitPRParser
  func parsePRInfo(_ json) → GitRepositoryService.P...
  func parsePRInfo(_ json) → GitRepositoryService.P...
  func parseStatusChecks(_ rollup) → GitRepositoryService.P...
  func parsePRInfoMatchingHeadSha(_ json, headSha) → GitRepositoryService.P...
  func parsePRList(_ json) → [GitRepositoryService....
```

### Muxy/Services/Git/GitProcessRunner.swift
```
struct GitProcessResult
enum GitProcessError
enum GitProcessRunner
  func resolveExecutable(_ name) → String?
  async func runGit(repoPath, arguments, lineLimit) → GitProcessResult
  async func runCommand(executable, arguments, workingDirectory) → GitProcessResult
  async func offMain(_ work) → T) async -> T
  async func offMainThrowing(_ work) → T) async throws -> T
```

### Muxy/Services/Git/GitRepositoryService.swift
```
struct GitRepositoryService
  async func currentBranch(repoPath) → String
```

### Muxy/Services/Git/GitSignpost.swift
```
enum GitSignpost
  func begin(_ name, _ message) → String = "") -> OSSign...
  func end(_ name, _ id, _ message) → String = "")
  func event(_ name, _ message) → String = "")
```

### Muxy/Services/Git/GitStatusParser.swift
```
enum GitStatusParser
  func parseStatusPorcelain(_ data, stats) → [GitStatusFile]
  func parseNumstat(_ output) → [String: NumstatEntry]
  func normalizeNumstatPath(_ rawPath) → String
```

### Muxy/Services/Git/GitWorktreeService.swift
```
struct GitWorktreeRecord
protocol GitWorktreeListing
  async func listWorktrees(repoPath) → [GitWorktreeRecord]
actor GitWorktreeService
  async func isGitRepository(_ path) → Bool
  async func hasUncommittedChanges(worktreePath) → Bool
  async func listWorktrees(repoPath) → [GitWorktreeRecord]
  async func addWorktree(repoPath, path, branch, createBranch)
  async func removeWorktree(repoPath, path, force)
  async func deleteBranch(repoPath, branch, force)
  func flush()
```

### Muxy/Services/Git/VCSPersistedSettings.swift
```
enum VCSPersistedSettings
  func loadSectionVisibility(repoPath) → SectionVisibility
  func storeSectionVisibility(_ visibility, repoPath)
  func loadAutoSyncMinutes(repoPath) → Int
  func storeAutoSyncMinutes(_ minutes, repoPath)
  func clearSettings(repoPath)
  func loadSectionCollapse(repoPath) → SectionCollapse
  func storeSectionCollapse(_ collapse, repoPath)
```

### Muxy/Services/JSONFilePersistence.swift
```
enum MuxyFileStorage
  func fileURL(filename) → URL
  func appSupportDirectory() → URL
  func worktreeRoot(forProjectID projectID) → URL
  func worktreeDirectory(forProjectID projectID, name) → URL
```

### Muxy/Services/KeyBindingPersistence.swift
```
protocol KeyBindingPersisting
  func loadBindings() → [KeyBinding]
  func saveBindings(_ bindings)
class FileKeyBindingPersistence
  func loadBindings() → [KeyBinding]
  func saveBindings(_ bindings)
  func encode(to encoder)
```

### Muxy/Services/KeyBindingStore.swift
```
class KeyBindingStore
  func binding(for action) → KeyBinding
  func combo(for action) → KeyCombo
  func updateBinding(action, combo)
  func resetToDefaults()
  func resetBinding(action)
  func isRegisteredShortcut(event, scopes) → Bool
  func action(for event, scopes) → ShortcutAction?
  func conflictingAction(for combo, excluding) → ShortcutAction?
```

### Muxy/Services/KimiUsageParser.swift
```
enum KimiUsageParserError
enum KimiUsageParser
  func parse(from data) → ParsedUsage
```

### Muxy/Services/MiniMaxUsageParser.swift
```
enum MiniMaxUsageParserError
enum MiniMaxUsageParser
  func parseMetricRows(from data) → [AIUsageMetricRow]
  func parseMetricRows(from data, region) → [AIUsageMetricRow]
```

### Muxy/Services/MobileServerService.swift
```
class MobileServerService
  func configure(_ delegateBuilder) → MuxyRemoteServerDelegate)
  func setEnabled(_ enabled)
  func stop()
  func stopForTermination()
  func isValid(port) → Bool
```

### Muxy/Services/ModifierKeyMonitor.swift
```
class ModifierKeyMonitor
  func start()
  func stop()
  func isHolding(modifiers) → Bool
```

### Muxy/Services/MuxyNotificationHooks.swift
```
enum MuxyNotificationHooks
  func scriptPath(named name, extension ext) → String?
```

### Muxy/Services/NotificationNavigator.swift
```
struct NavigationContext
enum NotificationNavigator
  func resolveContext(for paneID, appState, worktreeStore) → NavigationContext?
  func navigate(to notification, appState, notificationStore)
  func activeTabID(appState) → UUID?
  func isActiveTab(_ tabID, appState) → Bool
```

### Muxy/Services/NotificationStore.swift
```
class NotificationStore
  func unreadCount(for projectID) → Int
  func unreadCount(for projectID, worktreeID) → Int
  func hasUnread(tabID) → Bool
  func markAsRead(tabID)
  func add(paneID, source, title, body, appState)
  func addWithContext(context, source, title, body, appState)
```

### Muxy/Services/PairingRequestCoordinator.swift
```
struct PairingRequest
class PairingRequestCoordinator
  async func requestApproval(deviceID, deviceName, token) → Bool
  func approve(_ request)
  func deny(_ request)
```

### Muxy/Services/PaneOwnershipStore.swift
```
class PaneOwnershipStore
  func owner(for paneID) → PaneOwnerDTO
  func isOwnedByMac(_ paneID) → Bool
  func isOwnedBy(clientID, paneID) → Bool
  func remoteOwner(for paneID) → UUID?
  func registerDevice(clientID, name)
  func assign(paneID, to clientID)
  func releaseToMac(paneID)
  func releaseAll(clientID)
```

### Muxy/Services/ProjectLogoStorage.swift
```
enum ProjectLogoStorage
  func save(croppedImage image, forProjectID projectID) → String?
  func logoPath(for filename) → String
  func remove(forProjectID projectID)
```

### Muxy/Services/ProjectPersistence.swift
```
protocol ProjectPersisting
  func loadProjects() → [Project]
  func saveProjects(_ projects)
class FileProjectPersistence
  func loadProjects() → [Project]
  func saveProjects(_ projects)
```

### Muxy/Services/ProjectStore.swift
```
class ProjectStore
  func add(_ project)
  func remove(id)
  func rename(id, to newName)
  func setLogo(id, to logo)
  func setIconColor(id, to color)
  func reorder(fromOffsets source, toOffset destination)
  func save()
```

### Muxy/Services/Providers/AmpUsageProvider.swift
```
struct AmpUsageProvider
  async func fetchUsageSnapshot() → AIProviderUsageSnapshot
  func readToken(env, homeDirectory)
```

### Muxy/Services/Providers/ClaudeCodeProvider.swift
```
struct ClaudeCodeProvider
  func isToolInstalled() → Bool
  func install(hookScriptPath)
  func uninstall()
```

### Muxy/Services/Providers/CodexProvider.swift
```
struct CodexProvider
  func isToolInstalled() → Bool
  func install(hookScriptPath)
  func uninstall()
```

### Muxy/Services/Providers/CodexUsageProvider.swift
```
struct CodexUsageProvider
  async func fetchUsageSnapshot() → AIProviderUsageSnapshot
  func readAuth(env, homeDirectory)
```

### Muxy/Services/Providers/CopilotUsageProvider.swift
```
struct CopilotUsageProvider
  async func fetchUsageSnapshot() → AIProviderUsageSnapshot
  func readToken(env, keychainReader) → String?)? = nil,
```

### Muxy/Services/Providers/CursorProvider.swift
```
struct CursorProvider
  func install(hookScriptPath)
  func uninstall()
```

### Muxy/Services/Providers/CursorUsageProvider.swift
```
struct CursorUsageProvider
```

### Muxy/Services/Providers/FactoryUsageProvider.swift
```
struct FactoryUsageProvider
  async func fetchUsageSnapshot() → AIProviderUsageSnapshot
```

### Muxy/Services/Providers/KimiUsageProvider.swift
```
struct KimiUsageProvider
  async func fetchUsageSnapshot() → AIProviderUsageSnapshot
```

### Muxy/Services/Providers/MiniMaxUsageProvider.swift
```
enum MiniMaxRegion
struct MiniMaxUsageProvider
  async func fetchUsageSnapshot() → AIProviderUsageSnapshot
  func readToken(env, homeDirectory)
```

### Muxy/Services/Providers/OpenCodeProvider.swift
```
struct OpenCodeProvider
  func isToolInstalled() → Bool
  func install(hookScriptPath)
  func uninstall()
```

### Muxy/Services/Providers/ZaiUsageProvider.swift
```
struct ZaiUsageProvider
  async func fetchUsageSnapshot() → AIProviderUsageSnapshot
  func readToken(env) → String
```

### Muxy/Services/RemoteServerDelegate.swift
```
class RemoteServerDelegate
  func listProjects() → [ProjectDTO]
  func selectProject(_ projectID)
  func listWorktrees(projectID) → [WorktreeDTO]
  func selectWorktree(projectID, worktreeID)
  func getWorkspace(projectID) → WorkspaceDTO?
  func createTab(projectID, areaID, kind) → TabDTO?
  func closeTab(projectID, areaID, tabID)
  func selectTab(projectID, areaID, tabID)
```

### Muxy/Services/RemoteTerminalSnapshotBuilder.swift
```
enum RemoteTerminalSnapshotBuilder
  func buildBytes(from snapshot) → Data
```

### Muxy/Services/RemoteTerminalStreamer.swift
```
class RemoteTerminalStreamer
  func attach(paneID, surface)
  func detach(paneID, surface)
```

### Muxy/Services/ShellEscaper.swift
```
enum ShellEscaper
  func escape(_ path) → String
```

### Muxy/Services/ShortcutActionDispatcher.swift
```
struct ShortcutActionDispatcher
  func perform(_ action, activeProject, openVCS) → Void) -> Bool
```

### Muxy/Services/ShortcutContext.swift
```
enum ShortcutContext
  func isMainWindow(_ window) → Bool
  func activeScopes(for window) → Set<ShortcutScope>
```

### Muxy/Services/WorkspacePersistence.swift
```
protocol WorkspacePersisting
  func loadWorkspaces() → [WorkspaceSnapshot]
  func saveWorkspaces(_ workspaces)
class FileWorkspacePersistence
  func loadWorkspaces() → [WorkspaceSnapshot]
  func saveWorkspaces(_ workspaces)
```

### Muxy/Services/WorktreePersistence.swift
```
protocol WorktreePersisting
  func loadWorktrees(projectID) → [Worktree]
  func saveWorktrees(_ worktrees, projectID)
  func removeWorktrees(projectID)
class FileWorktreePersistence
  func loadWorktrees(projectID) → [Worktree]
  func saveWorktrees(_ worktrees, projectID)
  func removeWorktrees(projectID)
```

### Muxy/Services/WorktreeSetupRunner.swift
```
enum WorktreeSetupRunner
  async func run(sourceProjectPath, paneID)
```

### Muxy/Services/WorktreeStore.swift
```
class WorktreeStore
  func loadAll(projects)
  func ensurePrimary(for project)
  func list(for projectID) → [Worktree]
  func projectID(forWorktreePath path) → UUID?
  func primary(for projectID) → Worktree?
  func worktree(projectID, worktreeID) → Worktree?
  func preferred(for projectID, matching preferredID) → Worktree?
  func add(_ worktree, to projectID)
```

### Muxy/Services/ZaiUsageParser.swift
```
enum ZaiUsageParserError
enum ZaiUsageParser
  func parseMetricRows(quotaData) → [AIUsageMetricRow]
  func parsePlanName(subscriptionData) → String?
```

### Muxy/Syntax/Grammars/CFamilyGrammars.swift
```
struct CLikeBlockComment
```

### Muxy/Syntax/MarkdownInlineHighlighter.swift
```
struct MarkdownInlineDecoration
enum MarkdownInlineHighlighter
  func decorations(line, isInsideFencedCode) → [MarkdownInlineDecorat...
enum MarkdownInlineStyle
  func headingFontSize(baseSize, level) → CGFloat
  func font(for kind, baseFont) → NSFont?
  func foregroundColor(for kind) → NSColor?
  func strikethroughStyle(for kind) → NSUnderlineStyle?
```

### Muxy/Syntax/SyntaxGrammar.swift
```
enum LineEndState
struct SyntaxGrammar
```

### Muxy/Syntax/SyntaxHTMLRenderer.swift
```
enum SyntaxHTMLRenderer
  func render(source, grammar) → String
  func cssClass(for scope) → String
  func cssStylesheet(background, foreground) → String
```

### Muxy/Syntax/SyntaxHighlighter.swift
```
class SyntaxHighlighter
  func reset()
  func invalidate(fromLine index)
  func tokens(forLine line) → [TokenSpan]?
  func lineStartState(at line) → LineEndState
  func applyEdit(startLine, oldLineCount, newLineCount, backingStore) → EditOutcome
  func spans(in range, lineStartOffsets, backingStore) → [AppliedSpan]
```

### Muxy/Syntax/SyntaxTokenizer.swift
```
struct TokenSpan
struct SyntaxTokenizer
  func tokenize(line, startState) → (tokens: [TokenSpan], ...
```

### Muxy/Views/Components/AppBundleIconView.swift
```
struct AppBundleIconView
```

### Muxy/Views/Components/FileDiffIcon.swift
```
struct FileDiffIcon
  func path(in rect) → Path
  func p(_ x, _ y) → CGPoint
struct FileDiffIconButton
```

### Muxy/Views/Components/FileTreeIcon.swift
```
struct FileTreeIconButton
```

### Muxy/Views/Components/IconButton.swift
```
struct IconButton
```

### Muxy/Views/Components/MiddleClickView.swift
```
struct MiddleClickView
  func makeNSView(context) → MiddleClickNSView
  func updateNSView(_ nsView, context)
class MiddleClickNSView
```

### Muxy/Views/Components/NotificationBadge.swift
```
struct NotificationBadge
```

### Muxy/Views/Components/PaletteOverlay.swift
```
struct PaletteOverlay
struct PaletteSearchField
  func makeCoordinator() → Coordinator
  func makeNSView(context) → NSTextField
  func updateNSView(_ nsView, context)
  func controlTextDidChange(_ obj)
  func control(_ control, textView _, doCommandBy commandSelector) → Bool
```

### Muxy/Views/Components/PopoverPicker.swift
```
struct PopoverFooterAction
struct PopoverPicker
```

### Muxy/Views/Components/QuickOpenOverlay.swift
```
struct QuickOpenOverlay
```

### Muxy/Views/Components/SearchableListPicker.swift
```
struct SearchableListPicker
```

### Muxy/Views/Components/SegmentedPicker.swift
```
struct SegmentedPicker
```

### Muxy/Views/Components/UUIDFramePreferenceKey.swift
```
struct UUIDFramePreferenceKey
  func reduce(value, nextValue) → [UUID: CGRect])
enum SidebarFrameTag
enum AreaFrameTag
enum TabFrameTag
```

### Muxy/Views/Components/WindowDragView.swift
```
struct WindowDragRepresentable
  func makeNSView(context) → WindowDragView
  func updateNSView(_ nsView, context)
class WindowDragView
```

### Muxy/Views/Components/WorktreeSwitcherOverlay.swift
```
struct WorktreeSwitcherOverlay
struct WorktreeSwitcherItem
```

### Muxy/Views/Editor/EditorSearchBar.swift
```
struct EditorSearchBar
```

### Muxy/Views/Editor/Extensions/EditorExtension.swift
```
protocol EditorExtension
  func didMount(context)
  func willUnmount(context)
  func renderViewport(context, lineRange)
  func applyIncremental(context, lineRange, edit)
  func textDidChange(context)
```

### Muxy/Views/Editor/Extensions/EditorRenderContext.swift
```
struct EditorRenderContext
struct EditorTextEdit
```

### Muxy/Views/Editor/Extensions/MarkdownInlineExtension.swift
```
class MarkdownInlineExtension
  func renderViewport(context, lineRange)
  func applyIncremental(context, lineRange, edit _)
```

### Muxy/Views/Editor/Extensions/SyntaxHighlightExtension.swift
```
class SyntaxHighlightExtension
  func renderViewport(context, lineRange)
  func applyIncremental(context, lineRange, edit)
protocol SyntaxHighlightCoordinator
  func scheduleSyntaxCascadeReapply()
```

### Muxy/Views/Editor/History/ViewportEditHistory.swift
```
struct ViewportCursor
struct PendingViewportEdit
struct ViewportEdit
struct ViewportEditGroup
protocol ViewportEditHistoryHost
  func adjustViewportRangeForReplacement(startLine, replacedLineCount, insertedLineCount)
  func invalidateSyntaxHighlightsFromLine(_ line)
  func invalidateRenderedViewportText()
  func scheduleMarkdownPreviewRefresh(immediate)
  func applyHistorySelection(_ selection)
class ViewportEditHistory
  func clear()
  func push(_ edit)
  func performUndo() → Bool
  func performRedo() → Bool
```

### Muxy/Views/Editor/Markdown/MarkdownScrollSyncController.swift
```
class MarkdownScrollSyncController
  func attach(scrollView, viewport)
  func updateEditorScrollMetrics()
  func syncScrollPositionIfNeeded(refreshViewport) → Void, rebuildLineStart...
  func updatePreviewSyncPointFromEditorScroll()
  func reconcileScrollBoundsChange()
  func publishProgressIfEditorAutoScrolled(_ work) → Void)
```

### Muxy/Views/EmptyProjectPlaceholder.swift
```
struct EmptyProjectPlaceholder
```

### Muxy/Views/Markdown/MarkdownAssetSchemeHandler.swift
```
class MarkdownAssetSchemeHandler
  func webView(_, start urlSchemeTask)
  func webView(_, stop _)
```

### Muxy/Views/Markdown/MarkdownLocalImageSchemeHandler.swift
```
class MarkdownLocalImageSchemeHandler
  func webView(_, start urlSchemeTask)
  func webView(_, stop _)
  func encodedURL(forBaseDirectory directory, relativePath) → URL?
```

### Muxy/Views/Markdown/MarkdownPreviewAnchorGeometryBridge.swift
```
enum MarkdownPreviewAnchorGeometryBridge
```

### Muxy/Views/Markdown/MarkdownRemoteImageSchemeHandler.swift
```
class MarkdownRemoteImageSchemeHandler
  func webView(_, start urlSchemeTask)
```

### Muxy/Views/Markdown/MarkdownTabView.swift
```
struct MarkdownPreviewScrollReport
struct MarkdownWebView
  func makeCoordinator() → Coordinator
  func makeNSView(context) → WKWebView
  func updateNSView(_ webView, context)
  func dismantleNSView(_ webView, coordinator)
```

### Muxy/Views/Markdown/MarkdownWebBridge.swift
```
enum MarkdownWebBridge
  func scrollToTopScript(_ scrollTop) → String
```

### Muxy/Views/Markdown/PathBreadcrumb.swift
```
struct PathBreadcrumb
```

### Muxy/Views/NotificationPanel.swift
```
struct NotificationPanelItem
struct NotificationPanel
```

### Muxy/Views/ProviderIconView.swift
```
struct ProviderIconView
```

### Muxy/Views/Settings/AIUsageSettingsView.swift
```
struct AIUsageSettingsView
```

### Muxy/Views/Settings/EditorSettingsView.swift
```
struct EditorSettingsView
```

### Muxy/Views/Settings/KeyboardShortcutsSettingsView.swift
```
struct KeyboardShortcutsSettingsView
```

### Muxy/Views/Settings/MobileSettingsView.swift
```
struct MobileSettingsView
```

### Muxy/Views/Settings/NotificationSettingsView.swift
```
struct NotificationSettingsView
```

### Muxy/Views/Settings/SettingsComponents.swift
```
enum SettingsMetrics
struct SettingsContainer
struct SettingsSection
struct SettingsRow
struct SettingsToggleRow
```

### Muxy/Views/Settings/ShortcutBadge.swift
```
struct ShortcutBadge
```

### Muxy/Views/Settings/ShortcutRecorderView.swift
```
struct ShortcutRecorderView
  func makeNSView(context) → ShortcutRecorderNSView
  func updateNSView(_ nsView, context)
class ShortcutRecorderNSView
```

### Muxy/Views/Sidebar/AIUsagePanel.swift
```
struct AIUsagePreviewButton
struct AIUsagePanel
struct AIProviderUsageView
struct AIUsageMetricRowView
```

### Muxy/Views/Sidebar/CreateWorktreeSheet.swift
```
enum CreateWorktreeResult
struct CreateWorktreeSheet
```

### Muxy/Views/Sidebar/LogoCropperSheet.swift
```
struct LogoCropperSheet
```

### Muxy/Views/Sidebar/ProjectIconColorPicker.swift
```
struct ProjectIconColorPicker
```

### Muxy/Views/Sidebar/WorktreeRefreshHelper.swift
```
enum WorktreeRefreshHelper
  async func refresh(project, appState, worktreeStore, isRefreshing)
  func presentError(_ message)
```

### Muxy/Views/Terminal/TerminalPane.swift
```
struct TerminalPane
struct RemoteControlledPlaceholder
struct TerminalBridge
  func makeCoordinator() → Coordinator
  func makeNSView(context) → GhosttyTerminalNSView
  func updateNSView(_ nsView, context)
```

### Muxy/Views/Terminal/TerminalSearchBar.swift
```
struct TerminalSearchBar
```

### Muxy/Views/VCS/BranchPicker.swift
```
struct BranchPicker
struct BranchPickerContent
```

### Muxy/Views/VCS/CommitHistoryView.swift
```
struct CommitHistoryView
```

### Muxy/Views/VCS/CreateBranchSheet.swift
```
struct CreateBranchSheet
```

### Muxy/Views/VCS/CreatePRSheet.swift
```
struct CreatePRForm
```

### Muxy/Views/VCS/DiffBodyView.swift
```
struct DiffBodyView
```

### Muxy/Views/VCS/DiffViewerPane.swift
```
struct DiffViewerPane
```

### Muxy/Views/VCS/PullRequestsListView.swift
```
struct PullRequestsListView
struct PullRequestRow
struct PullRequestsAutoSyncMenu
```

### Muxy/Views/VCS/VCSTabView.swift
```
struct VCSTabView
struct VCSSectionVisibilityMenu
struct PRPill
struct PRPopover
```

### Muxy/Views/VCS/VCSWindowView.swift
```
struct VCSWindowView
```

### Muxy/Views/VCS/WorktreeBranchPicker.swift
```
struct WorktreeBranchPicker
```

### Muxy/Views/WelcomeView.swift
```
struct WelcomeView
```

### Muxy/Views/Workspace/DropZoneOverlay.swift
```
struct DropZoneHighlight
```

### Muxy/Views/Workspace/PaneNode.swift
```
struct PaneNode
```

### Muxy/Views/Workspace/SplitContainer.swift
```
struct SplitContainer
```

### Muxy/Views/Workspace/Workspace.swift
```
struct TerminalArea
```

### Muxy/Views/MainWindow/CloseConfirmationKind.swift
```
enum CloseConfirmationKind
```

### Muxy/Views/MainWindow/WindowTitleUpdater.swift
```
struct WindowTitleUpdater
  func makeNSView(context) → NSView
  func updateNSView(_ nsView, context)
```

### Muxy/Views/MainWindow/FileTreeSelectionSync.swift
```
struct FileTreeSelectionSync
  func body(content) → some View
```

### Muxy/Views/MainWindow/NavigationArrowButton.swift
```
struct NavigationArrowButton
```

### Muxy/Views/MainWindow/MainWindowShortcutInterceptor.swift
```
struct MainWindowShortcutInterceptor
  func makeNSView(context) → ShortcutInterceptingView
  func updateNSView(_ nsView, context)
class ShortcutInterceptingView
```

### Muxy/Views/Workspace/TabDragState.swift
```
struct TabDragState
```

### Muxy/Views/Workspace/TabWidthPreferenceKey.swift
```
struct TabWidthPreferenceKey
  func reduce(value, nextValue) → CGFloat)
```

### Muxy/Views/Sidebar/ExpandedNewWorktreeButton.swift
```
struct ExpandedNewWorktreeButton
```

### Muxy/Views/Sidebar/PrimaryBadge.swift
```
struct PrimaryBadge
```

### Muxy/Views/Sidebar/ExpandedRenamePopover.swift
```
struct ExpandedRenamePopover
```

### Muxy/Views/Sidebar/IdentifiableExpandedImage.swift
```
struct IdentifiableExpandedImage
```

### Muxy/Views/Sidebar/ProjectDragState.swift
```
struct ProjectDragState
```

### Muxy/Views/Sidebar/AddProjectButton.swift
```
struct AddProjectButton
```

### Muxy/Views/Sidebar/ProjectRowIdentifiableImage.swift
```
struct IdentifiableImage
```

### Muxy/Views/Sidebar/ProjectRowRenamePopover.swift
```
struct RenamePopover
```

### Muxy/Commands/MuxyCommands.swift
```
struct MuxyCommands
```

### Muxy/Models/AppState.swift
```
class AppState
  func restoreSelection(projects, worktrees)
```

### Muxy/Models/LayoutConfig.swift
```
struct LayoutDescriptor
struct LayoutConfig
  func directory(forProjectPath projectPath) → URL
  func discover(projectPath) → [LayoutDescriptor]
  func load(descriptor) → LayoutConfig?
  func load(projectPath, name) → LayoutConfig?
  func parse(_ value) → LayoutConfig?
```

### Muxy/Models/LayoutWorkspaceBuilder.swift
```
enum LayoutWorkspaceBuilder
  func build(config, projectPath) → Result?
```

### Muxy/Models/QuitConfirmationPreferences.swift
```
enum QuitConfirmationPreferences
```

### Muxy/Models/ViewportState.swift
```
class ViewportState
  func updateExtraDocumentHeight(_ value)
  func updateEstimatedLineHeight(font)
  func updateDocumentPadding(topInset, bottomInset, safetyPadding)
  func visibleLineRange(scrollY, visibleHeight) → Range<Int>
  func computeViewport(scrollY, visibleHeight) → Range<Int>
  func shouldUpdateViewport(scrollY, visibleHeight) → Bool
  func applyViewport(_ range)
  func viewportText() → String
```

### Muxy/Models/WorkspaceReducer.swift
```
struct WorkspaceState
struct WorkspaceSideEffects
enum WorkspaceReducer
  func reduce(action, state) → WorkspaceSideEffects
```

### Muxy/Models/WorkspaceReducer/ProjectLifecycleReducer.swift
```
enum ProjectLifecycleReducer
  func selectProject(projectID, worktreeID, worktreePath, state, effects)
  func removeProject(projectID, state, effects)
  func removeWorktree(projectID, worktreeID, replacement, state, effects)
  func cycleProject(projects, worktrees, forward, state, effects)
```

### Muxy/Models/WorkspaceReducer/WorkspaceReducerShared.swift
```
enum WorkspaceReducerShared
  func activeKey(projectID, state) → WorktreeKey?
  func resolveArea(key, areaID, state) → TabArea?
  func ensureWorkspaceExists(projectID, worktreeID, worktreePath, state, effects)
  func clearWorkspace(key, state)
  func handleProjectEmptiedIfNeeded(projectID, state, effects)
```

### Muxy/Services/GhosttyRuntimeEventAdapter.swift
```
protocol GhosttyRuntimeEventHandling
  func wakeup()
  func action(app, target, action) → Bool
  func readClipboard(userdata, location, state) → Bool
  func confirmReadClipboard(userdata, content, state)
  func writeClipboard(location, content, len)
  func closeSurface(userdata, needsConfirm)
class GhosttyRuntimeEventAdapter
  func wakeup()
  func action(app, target, action) → Bool
  func readClipboard(userdata, location, state) → Bool
  func confirmReadClipboard(userdata, content, state)
  func writeClipboard(location, content, len)
```

### Muxy/Services/GhosttyService.swift
```
class GhosttyService
  func applyInitialColorScheme()
  func paletteColor(at index) → NSColor?
```

### Muxy/Services/IDEIntegrationService.swift
```
class IDEIntegrationService
  func refreshInstalledApps()
  func openProject(at path, highlightingFileAt filePath, line, column, in ide) → Bool
```

### Muxy/Services/MuxyConfig.swift
```
class MuxyConfig
  func readGhosttyConfig() → String
  func writeGhosttyConfig(_ content)
  func updateConfigValue(_ key, value)
  func configValue(for key) → String?
```

### Muxy/Services/ThemeService.swift
```
enum ThemeAppearance
struct ThemePreview
struct ThemeSelection
  func resolvedName(isDark) → String?
class ThemeService
  async func loadThemes() → [ThemePreview]
  func currentThemeName() → String?
  func currentThemeSelection() → ThemeSelection?
  func currentLightThemeName() → String?
  func currentDarkThemeName() → String?
  func activeThemeName() → String?
  func activeThemePreview() → ThemePreview?
  func activeAppearance() → ThemeAppearance
```

### Muxy/Services/UpdateService.swift
```
enum UpdateChannel
class UpdateService
  func start()
  func checkForUpdates()
```

### Muxy/Syntax/SyntaxLanguageRegistry.swift
```
enum SyntaxLanguageRegistry
  func grammar(forFile filename) → SyntaxGrammar?
  func grammar(forLanguageHint hint) → SyntaxGrammar?
```

### Muxy/Syntax/SyntaxScope.swift
```
enum SyntaxScope
enum SyntaxTheme
  func color(for scope) → NSColor
```

### Muxy/Theme/EditorThemePalette.swift
```
struct EditorThemePalette
  func resolve(preview, fallbackBackground, fallbackForeground, fallbackAccent, fallbackPaletteColor) → NSColor?
  func paletteColor(at index) → NSColor?
```

### Muxy/Theme/MuxyTheme.swift
```
enum MuxyTheme
```

### Muxy/Views/Components/DoubleClickView.swift
```
struct DoubleClickView
  func makeNSView(context) → DoubleClickNSView
  func updateNSView(_ nsView, context)
class DoubleClickNSView
```

### Muxy/Views/Editor/CodeEditorRepresentable.swift
```
class ViewportContainerView
struct CodeEditorView
  func makeCoordinator() → Coordinator
  func makeNSView(context) → NSScrollView
```

### Muxy/Views/Editor/EditorPane.swift
```
struct EditorPane
```

### Muxy/Views/Editor/Search/SearchController.swift
```
protocol SearchControllerHost
  func charOffsetForLocalLine(_ localLine) → Int
  func refreshViewport(force)
  func reapplySyntaxHighlights()
  func invalidateSyntaxHighlightsFromLine(_ line)
  func invalidateRenderedViewportText()
  func clearViewportHistory()
  func scheduleMarkdownPreviewRefresh(immediate)
  func recordHighlightTiming(start, highlightedRangeCount, force)
class SearchController
  func clearHighlights()
  func applyHighlights(force)
  func performSearch(_ needle, caseSensitive, useRegex)
```

### Muxy/Views/Help/HelpView.swift
```
enum HelpSection
struct HelpView
enum HelpLinks
  func openRepo()
  func openDocs()
  func openMobileRepo()
  func openDiscord()
  func openIssues()
  func openDoc(_ relativePath)
```

### Muxy/Views/Settings/AppearanceSettingsView.swift
```
struct AppearanceSettingsView
```

### Muxy/Views/Settings/GeneralSettingsView.swift
```
enum GeneralSettingsKeys
struct GeneralSettingsView
```

### Muxy/Views/Sidebar/WorktreePopover.swift
```
struct WorktreePopover
```

### Muxy/Views/Terminal/GhosttyTerminalNSView.swift
```
class GhosttyTerminalNSView
  func createSurface()
```

### Muxy/Views/Terminal/TerminalViewRegistry.swift
```
class TerminalViewRegistry
  func isOwnedByRemote(_ paneID) → Bool
  func view(for paneID, workingDirectory, command, commandInteractive) → GhosttyTerminalNSView
  func existingView(for paneID) → GhosttyTerminalNSView?
  func removeView(for paneID)
  func needsConfirmQuit(for paneID) → Bool
  func view(for paneID) → GhosttyTerminalNSView?
  func paneID(for view) → UUID?
  func applyColorSchemeToAllViews(isDark)
```

### Muxy/Views/ThemePicker.swift
```
enum ThemePickerMode
struct ThemePicker
```

### Muxy/Views/VCS/DiffComponents.swift
```
struct DiffSectionDivider
enum DiffBackgroundSide
func hunkLabel(_ raw) → String
func lineNumberWidth(for maxLineNumber) → CGFloat
func maxLineNumber(in rows) → Int
```

### Muxy/Views/VCS/DiffTextBridge.swift
```
enum DiffChunk
struct DiffContentBridge
  func makeCoordinator() → Coordinator
  func makeNSView(context _) → DiffContentNSView
  func updateNSView(_ nsView, context)
struct DiffGutterBridge
  func makeCoordinator() → Coordinator
  func makeNSView(context) → DiffGutterNSView
  func updateNSView(_ nsView, context)
func buildDiffChunks(from rows) → [DiffChunk]
func buildDiffMetadata(from rows) → [DiffLineMetadata]
func buildDiffAttributedString(from rows, theme) → DiffRenderedBlock
```

### Muxy/Views/VCS/DiffTextView.swift
```
struct DiffLineMetadata
enum DiffGutterMode
class DiffBackgroundLayoutManager
enum DiffMetrics
  func expectedWidth(maxColumns) → CGFloat
class DiffContentNSView
  func prepareSize(rowCount, maxColumns, lineHeight)
  func configure(attributedString, metadata, lineBackgrounds, lineHeight)
class DiffGutterNSView
func buildLineBackgrounds(metadata, side, theme) → [NSColor?]
```

### Muxy/Views/VCS/SplitDiffView.swift
```
struct SplitDiffView
enum SplitDiffChunk
struct SplitDiffPairedRow
  func pair(_ rows) → [SplitDiffPairedRow]
func buildSplitDiffChunks(from rows) → [SplitDiffChunk]
```

### Muxy/Views/VCS/UnifiedDiffView.swift
```
struct UnifiedDiffView
```

### Muxy/Views/Workspace/LayoutPickerMenu.swift
```
struct LayoutPickerMenu
```

### Muxy/Views/Workspace/TabAreaView.swift
```
struct TabAreaView
```

### Muxy/Views/Workspace/TabCell.swift
```
struct TabCell
```

### Muxy/Views/MainWindow/WindowOpenReceiver.swift
```
struct WindowOpenReceiver
```

### Muxy/Views/Sidebar/ExpandedWorktreeRow.swift
```
struct ExpandedWorktreeRow
```

### Muxy/Views/Sidebar.swift
```
enum SidebarLayout
  func resolvedWidth(expanded, collapsedStyle, expandedStyle) → CGFloat
  func isWide(expanded, expandedStyle) → Bool
  func isHidden(expanded, collapsedStyle) → Bool
struct Sidebar
```

### Muxy/Models/FileTreeState.swift
```
class FileTreeState
  func loadRootIfNeeded()
  func refresh()
  func refreshDirectory(path)
  func expand(path)
  func parentDirectory(of path) → String
  func toggle(_ entry)
  func isExpanded(_ entry) → Bool
  func childrenOf(_ entry) → [FileTreeEntry]?
```

### Muxy/Models/VCSTabState.swift
```
class VCSTabState
enum VCSFileTree
  func rows(files, expandedFolders) → [Row]
```

### Muxy/MuxyApp.swift
```
struct MuxyApp
class AppDelegate
  func handleOpenProjectPath(_ path)
  func flushPendingOpens()
  func application(_ application, open urls)
  func applicationDidFinishLaunching(_ notification)
struct WindowConfigurator
  func makeCoordinator() → Coordinator
  func makeNSView(context) → NSView
  func updateNSView(_ nsView, context)
  func neutralizeSafeAreaInsets(in window)
  func hideTitlebarDecorationView(in window)
  func interceptCloseButton(in window, coordinator)
  func repositionTrafficLights(in window)
  func handleCloseButton(_)
```

### Muxy/Resources/scripts/muxy-claude-hook.sh
```
function send_notification()
function extract_last_message()
```

### Muxy/Views/Components/DebugButton.swift
```
struct DebugButton
```

### Muxy/Views/Components/HealthIconButton.swift
```
struct HealthIconButton
```

### Muxy/Views/Components/OpenInIDEControl.swift
```
struct OpenInIDEControl
```

### Muxy/Views/Components/PipelineIconButton.swift
```
struct PipelineIconButton
```

### Muxy/Views/Components/UpdateBadge.swift
```
struct UpdateBadge
```

### Muxy/Views/FileTree/FileTreeCommands.swift
```
class FileTreeCommands
  func effectiveTargets(primaryPath) → [String]
  func beginNewFile(in directoryPath)
  func beginNewFolder(in directoryPath)
  func commitNewEntry(name)
  func cancelNewEntry()
  func beginRename(path)
  func commitRename(originalPath, newName)
  func cancelRename()
```

### Muxy/Views/FileTree/FileTreeView.swift
```
struct FileTreeView
```

### Muxy/Views/Settings/SettingsView.swift
```
struct SettingsView
```

### Muxy/Views/Sidebar/ExpandedProjectRow.swift
```
struct ExpandedProjectRow
```

### Muxy/Views/Sidebar/ProjectRow.swift
```
struct ProjectRow
```

### Muxy/Views/Sidebar/SidebarFooter.swift
```
struct SidebarFooter
```

### Muxy/Views/Workspace/TabStrip.swift
```
struct PaneTabStrip
  func snapshots(from tabs) → [TabSnapshot]
```

### Muxy/Services/ShellRunner.swift
```
struct ShellResult
enum ShellRunner
  func run(_ command, workingDirectory, timeout) → ShellResult
  func runAction(_ command, workingDirectory) → Bool
  func runWithOutput(_ command, workingDirectory) → String
  func runTool(_ name, arguments) → (installed: Bool, vers...
```

### Muxy/Models/PipelineState.swift
```
struct PipelineStep
enum StepKind
enum StepSeverity
enum StepStatus
struct WorkflowDef
struct PipelineRunRecord
struct StepResultRecord
```

### Muxy/Models/ProjectHealthState.swift
```
enum ItemStatus
enum CIStatusType
struct WorkflowItem
struct GlobalToolItem
struct CIStatus
struct PiExtensionEvent
```

### Muxy/Resources/scripts/validate-workflow.sh
```
# Validate workflow tooling configuration against the actual project.
function pass()
function fail()
function warn()
function detect_stack()
```

### Muxy/Services/ProjectOpenService.swift
```
enum ProjectOpenService
  func openProject(appState, projectStore, worktreeStore)
```

### Muxy/Views/Health/PipelinePanel.swift
```
struct PipelinePanel
```

### Muxy/Views/Health/ProjectHealthPanel.swift
```
struct ProjectHealthPanel
```

### Muxy/Views/MainWindow.swift
```
struct MainWindow
```

## MuxyServer

### MuxyServer/MuxyRemoteServer.swift
```
enum DeviceAuthDecision
enum MuxyRemoteServerError
protocol MuxyRemoteServerDelegate
  func listProjects() → [ProjectDTO]
  func selectProject(_ projectID)
  func listWorktrees(projectID) → [WorktreeDTO]
  func selectWorktree(projectID, worktreeID)
  func getWorkspace(projectID) → WorkspaceDTO?
  func createTab(projectID, areaID, kind) → TabDTO?
  func closeTab(projectID, areaID, tabID)
  func selectTab(projectID, areaID, tabID)
```

## MuxyShared

### MuxyShared/MuxyMessage.swift
```
enum MuxyMessage
  func encode(to encoder)
enum MuxyCodec
  func encode(_ message) → Data
  func decode(_ data) → MuxyMessage
```

### MuxyShared/MuxyProtocol.swift
```
enum MuxyMessageType
struct MuxyRequest
struct MuxyResponse
struct MuxyEvent
struct MuxyError
enum MuxyMethod
enum MuxyParams
enum MuxyResult
  func encode(to encoder)
enum MuxyEventKind
enum MuxyEventData
  func encode(to encoder)
```

### MuxyShared/NotificationDTO.swift
```
struct NotificationDTO
```

### MuxyShared/ProjectDTO.swift
```
struct ProjectDTO
```

### MuxyShared/ProjectIconColor.swift
```
enum ProjectIconColor
  func swatch(for identifier) → Swatch?
  func rgb(fromHex hex) → (Double, Double, Double)?
```

### MuxyShared/ProtocolParams.swift
```
struct SelectProjectParams
struct ListWorktreesParams
struct SelectWorktreeParams
struct GetWorkspaceParams
struct CreateTabParams
struct CloseTabParams
struct SelectTabParams
struct SplitAreaParams
enum SplitPositionDTO
struct CloseAreaParams
struct FocusAreaParams
struct TerminalInputParams
struct TerminalResizeParams
struct RegisterDeviceParams
struct PairDeviceParams
struct AuthenticateDeviceParams
struct PairingResultDTO
struct DeviceInfoDTO
enum PaneOwnerDTO
struct TakeOverPaneParams
struct ReleasePaneParams
struct PaneOwnershipEventDTO
struct DeviceThemeEventDTO
struct TerminalScrollParams
struct GetTerminalContentParams
```

### MuxyShared/VCSStatusDTO.swift
```
struct VCSStatusDTO
struct GitFileDTO
enum GitFileStatusDTO
struct VCSPullRequestDTO
struct VCSBranchesDTO
struct VCSCreatePRResultDTO
```

### MuxyShared/WorkspaceDTO.swift
```
struct WorkspaceDTO
enum SplitDirectionDTO
struct SplitBranchDTO
struct TabAreaDTO
struct TabDTO
enum TabKindDTO
```

### MuxyShared/WorktreeDTO.swift
```
struct WorktreeDTO
```

## mcp-servers

### mcp-servers/graphify-mcp.py
```
def graphify_run(*args, timeout)
def handle_initialize(req)
def handle_list_tools(req)
def handle_call_tool(req)
def error(req_id, msg)
def result(req_id, text)
def read_stdio()
def write_stdio(obj)
def main_stdio()
def main_http(port)
```

### mcp-servers/archon-mcp.py
```
def archon_run(*args, cwd, timeout)
def handle(req)
def call_tool(req)
def err_resp(req_id, msg)
def ok_resp(req_id, text)
def read_stdio()
def write_stdio(obj)
def main_stdio()
def main_http(port)
```

### mcp-servers/start-mcp.sh
```
# Start all MCP servers for the agent workflow.
```
