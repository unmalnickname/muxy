#!/bin/bash
set -e

REPO_ROOT=$(git rev-parse --show-toplevel)

if command -v graphify &>/dev/null && [ -d "$REPO_ROOT/.graphify" ]; then
    graphify scan "$REPO_ROOT" --quiet 2>/dev/null || true
fi
